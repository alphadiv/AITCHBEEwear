# Backend API URL (required in production when frontend is on a different host, e.g. Vercel).
# Leave empty for local dev (Vite proxies /api to backend).
# Example: https://your-backend.onrender.com
VITE_API_URL=

# Optional: Supabase anon/publishable key for future client-side Supabase usage.
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
